using Gtk 4.0;
using Adw 1;

template $KpTextView : Widget {
  focusable: true;
  valign: center;
  overflow: hidden;

  TextView text_view {
    editable: false;
    focusable: false;
    wrap-mode: word_char;
  }
}
